name: Sync from Gitee

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Add Gitee remote with token
        run: |
          git remote add gitee https://${{ secrets.GITEE_TOKEN }}@gitee.com/hoshinokennji/SiliconLifeCollective.git

      - name: Fetch from Gitee
        run: |
          git fetch gitee --tags --force
        env:
          GITEE_TOKEN: ${{ secrets.GITEE_TOKEN }}

      - name: Sync master branch
        run: |
          git checkout master
          git merge gitee/master --allow-unrelated-histories --strategy-option=theirs
          git push origin master --force

      - name: Sync all branches
        run: |
          for branch in $(git branch -r | grep 'gitee/' | grep -v 'HEAD' | sed 's/gitee\///'); do
            git checkout $branch 2>/dev/null || git checkout -b $branch gitee/$branch
            git push origin $branch --force
          done

      - name: Sync tags
        run: |
          git push origin --tags --force

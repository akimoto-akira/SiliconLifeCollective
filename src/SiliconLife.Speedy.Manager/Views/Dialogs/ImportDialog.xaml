<Window x:Class="SiliconLife.Speedy.Manager.Views.Dialogs.ImportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="导入文件"
        Width="500" Height="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Virtual Path Prefix -->
        <Label Grid.Row="0" Content="目标虚拟路径前缀:" Padding="0,0,0,2"/>
        <TextBox Grid.Row="1"
                 Text="{Binding VirtualPathPrefix, UpdateSourceTrigger=PropertyChanged}"
                 Padding="4,3"
                 Margin="0,0,0,8"/>

        <!-- File List Header -->
        <DockPanel Grid.Row="2" Margin="0,0,0,4">
            <Label Content="选择的文件:" Padding="0" DockPanel.Dock="Left"/>
            <Button Content="浏览..."
                    Command="{Binding BrowseFilesCommand}"
                    HorizontalAlignment="Right"
                    Padding="8,2"
                    DockPanel.Dock="Right"/>
        </DockPanel>

        <!-- File List -->
        <ListBox Grid.Row="3"
                 ItemsSource="{Binding SelectedFiles}"
                 Margin="0,0,0,8">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <DockPanel>
                        <Button Content="✖"
                                Command="{Binding DataContext.RemoveFileCommand,
                                    RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="{Binding}"
                                DockPanel.Dock="Right"
                                Padding="4,1"
                                Margin="4,0,0,0"
                                Background="Transparent"
                                BorderBrush="Transparent"/>
                        <TextBlock Text="{Binding}"
                                   VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis"/>
                    </DockPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Buttons -->
        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,4,0,0">
            <Button Content="导入"
                    Command="{Binding ConfirmCommand}"
                    Width="80"
                    Margin="0,0,8,0"
                    IsDefault="True"/>
            <Button Content="取消"
                    Command="{Binding CancelCommand}"
                    Width="80"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
